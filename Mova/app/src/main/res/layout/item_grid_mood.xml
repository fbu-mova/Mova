<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/cvMood"
        android:layout_width="@dimen/progressStackThickness"
        android:layout_height="@dimen/progressStackThickness"
        app:cardBackgroundColor="@color/blueMid"
        app:cardCornerRadius="@dimen/borderRadius"
        app:cardElevation="0dp" />

    <TextView
        android:id="@+id/tvDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/elementMargin"
        android:layout_weight="0"
        android:fontFamily="@font/lato_bold"
        android:text="X"
        android:textColor="@color/textLight" />
</LinearLayout>